<%= link_to article_path(article) do %>
  <div class="card-trip">
    <%= cl_image_tag article.photo, height: 300, width: 400, crop: :fill %>
    <div class="card-trip-infos">
      <div>
        <h2><%= article.name %></h2>
        <p> <%= article.description %></p>
      </div>
      <h2 class="card-trip-pricing"><%= article.price %>€ par jour</h2>
      <%= cl_image_tag article.user.picture, class: "avatar-bordered", id: "avatar-home", alt: "avatar-large" %>
    </div>
  </div>
<% end %>
